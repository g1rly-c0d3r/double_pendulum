program doublepen
  use types, only: dp
  use constants, only: PI
  implicit none
  real(dp) :: radius1, pos1, linvel1, radvel1, linaccel1, radaccel1
  real(dp) :: radius2, pos2, linvel2, radvel1, linaccel2, radaccel2
  real(dp) :: angle

  

end program doublepen

